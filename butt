#! /bin/sh

fail=0

for filename in test*; do
  if [ -d "$filename" ]; then
    continue
  fi
  if sh "$filename"; then
    echo -n "p "
  else
    echo -n "f "
    fail=1
  fi
  echo "$filename"
done

exit $fail
